# React Flow Regression: Shift+Scroll/Zoom Pan Logic

## Description
Holding `Shift` doesn't bypass zoom limitations, and scrolling/pan combinations are broken in Webviews when using external keys.

## Status
Todo

## Sub-tasks
- The `isSpacebarHeld` state works for Panning. We need to mirror this exact global event-listening strategy for `isShiftHeld`.
- Ensure `panOnScroll={true}` combined with custom modifiers works on the `<ReactFlow />` root.