# ReactFlow Bug: Minimap is Unclickable

## Description
The minimap perfectly mirrors the node locations, but clicking it no longer pans the screen to that point.

## Status
Todo

## Sub-tasks
- Use React Flow's `onNodeClick` inside the native `<MiniMap />` props.
- Call `setCenter(x, y)` hooked inside the generic minimap callback object.