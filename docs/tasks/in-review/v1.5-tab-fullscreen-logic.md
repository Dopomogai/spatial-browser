# Tab Fullscreen Mathematics
**Issue:** When clicking the green maximize button on a tab, it expands the physical webview to match `window.innerWidth`, however, the ReactFlow Canvas isn't physically focused or centering perfectly over the active widget.
**Resolution:** Re-run the bounding box math inside `handleFullscreenToggled` to ensure the specific `tabId` is focused at zoom `1` dynamically without user intervention.
