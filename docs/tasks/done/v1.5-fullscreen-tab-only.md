# Maximize Tab Minimized Issues
**Issue:** When clicking Fullscreen on a tab (Green button), "the tab is a bit too high and to the left and we need to hide a minimap once we are there".
**Resolution:** Re-map `BrowserWidgetNode.tsx` Green button or `handleFullscreenToggled` offset. Re-examine the math: `widget.position.x + (window.innerWidth / 2)`. It needs to account precisely for the tab coordinates. Also when `isFullscreen`, inject boolean state `isTabMaximized` to UI layer and add a class/conditional variable to hide `<MiniMap />` and `<TopTabBar />`.
