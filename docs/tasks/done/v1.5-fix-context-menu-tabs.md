# Canvas Menu Closes On WebViews
**Issue:** User reported: "almost there - everywhere exept for if you click inside the tabs" (Regarding context menu dismissal).
**Resolution:** Add a global `close-context-menu` event `onPointerDown` within `<webview>` container or track global window blurring to close `contextMenuOpen` in `SpatialCanvas.tsx`.
