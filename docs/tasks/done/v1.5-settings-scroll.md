# Settings Rescale / Scroll 
**Issue:** The settings pane is rendered too small, and lacks an inner scrollbar. 
**Resolution:** Increase initial `w` and `h` in `handleSpawnSettings` within `SpatialCanvas.tsx`. Inside `BrowserWidgetNode.tsx`, verify the `overflow-y-auto` rules on the internal `about:blank#settings` `<div>` structure allow standard scrolling.
