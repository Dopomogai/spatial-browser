# Dark/Light Theme Switching
**Issue:** The user implicitly prefers the "Warm light theme, NOT dark."
**Resolution:** Introduce a `theme` state inside `useCanvasStore.ts`. Map `data-theme` onto the `#root` wrapper in `index.html`. Introduce a theme toggle in the Settings.
