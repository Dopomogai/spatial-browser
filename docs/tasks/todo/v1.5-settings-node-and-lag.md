# Settings Widget & Context Menus Missing/Buggy

## Description
The Settings widget fails to open on click. Interacting with tabs feels buggy/laggy. The double Context Menu logic persists despite the change.

## Status
Todo

## Sub-tasks
- Build `SettingsNode.tsx` corresponding to `browser_widget`.
- Investigate `onClick` event propagation latency on `BrowserWidgetNode`.
- Natively intercept `e.preventDefault` on `<webview>` `context-menu` *via IPC mapping* back out to React flow context menu instead of swallowing it locally.