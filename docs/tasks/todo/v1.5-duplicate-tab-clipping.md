# Tab Duplicate Action Spawn Position Error
**Issue:** The user specified: "it works - although the spawn position is always same - no matter which tab I believe this is true for all new tabs as well".
**Resolution:** Add a new offset parameter directly mapping to the `positionX` / `positionY` values of the `Node` and make sure it is not accidentally pulling `0` or falling back to defaults if the payload strips them. Check `BrowserWidgetNode.tsx` duplicate click handler. Fix default `x` and `y` logic across newly spawned tools so they offset visually based on `BrowserWidgetNode.positionX`.
