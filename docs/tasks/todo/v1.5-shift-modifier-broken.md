# Shift Key Modifier Broken
**Issue:** Holding "Shift" has stopped working for its intended purpose (e.g., panning the canvas relative to webviews).
**Resolution:** Investigate the `keydown` and `keyup` listeners in `App.tsx` and the `shift-state-change` custom event dispatch. Ensure `useCanvasStore.ts` or related components correctly update state based on this event.
