# Minimal Top Bar Refactor
**Issue:** User feedback specified: "Top bar should be on off and minimal - minimal means without the tabs, but with a search and 3 buttons." The previous implementation simply hid the bar or kept the tabs.
**Actionable Fix:**
1. Update `TopTabBar.tsx` so when `isTopTabBarVisible` is TRUE, it does NOT render the array of tabs. It should ONLY render:
   - The Global Search button (Cmd+K icon)
   - The Context/Settings gear
   - The History icon
2. Ensure this aligns neatly with the spatial-glass aesthetic.
