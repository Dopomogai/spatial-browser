# Widget Spawn Centering
**Issue:** Settings and History widgets spawn in the top-left corner instead of the center of the screen/viewport.
**Resolution:** Update the math in `handleSpawnSettings` and `handleSpawnCanvases` in `SpatialCanvas.tsx`. `visibleX` and `visibleY` calculate the top-left offset; calculate the actual screen center dynamically and translate to the viewport scale.
