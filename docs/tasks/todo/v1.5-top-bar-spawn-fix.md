# Task: Fix Top Bar Tab Spawning Route

## Problem Statement
Spawning a newly tabbed browser window from the Canvas context menu works correctly, but invoking the global "Spawn" button from the main Top Bar does not trigger node creation.

## Hypothesis
The `TopTabBar.tsx` component is either missing the proper `onClick` hook to `useCanvasStore`, or it is dispatching an outdated event payload from the deprecated TLDraw architecture. 

## Action Plan
1. Inspect `src/components/TopTabBar.tsx` to identify the 'Add Tab / Spawn' button.
2. Inspect `src/store/useCanvasStore.ts` to identify the correct node insertion method (`addNode` / `spawnTab`).
3. Wire the Top Bar button to inject a new `browser-widget` AppNode into the React Flow array.
4. Set the default parameters so it spawns center-screen relative to the current viewport camera.
5. Commit and push the fix.
