# React Flow Regression: Node Resizing

## Description
Tabs can no longer be dynamically resized from their edges/corners. The underlying React Flow `data.w` and `data.h` state properties exist, but the visual resize handles were lost when we uninstalled TLDraw.

## Status
Todo

## Sub-tasks
- Import `<NodeResizer />` from `@xyflow/react`.
- Inject it into `BrowserWidgetNode.tsx`.
- Bind the `onResize` callback to our Zustand `updateWidgetData(id, { w: width, h: height })` function so the resizing persists to Supabase.