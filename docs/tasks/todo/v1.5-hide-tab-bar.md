# Hide/Show Tabs Bar Toggle
**Issue:** The user needs a defined setting to hide/show the top tabs bar.
**Resolution:** Add a global Zustand boolean `isTopTabBarVisible` to `useCanvasStore.ts`. Map it natively to a new setting in `about:blank#settings`, and conditionally render `<TopTabBar>` in `App.tsx`.
