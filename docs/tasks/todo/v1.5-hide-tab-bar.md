# Top Tabs Bar Toggling & Bubble Design
**Issue:** The user needs a setting to quickly toggle off the browser tabs but keep a minimized top bar with search, history, and settings visible. The tab items themselves need a redesign to look like beautiful bubbles, and group tab selection on the bar + canvas is required.
**Resolution:** Rework `TopTabBar.tsx` layout. Grouping on logic (drawing a region) maps to `ReactFlow` multi-select nodes. Tabs mapping to a `display: flex` gap or bubble layout CSS.
