# Unable to Spawn New Tabs
**Issue:** The user stated "tabs are not spawning... I can no longer add it - basically nothing happens on clicking anything from the canvases right click menu."
**Hypothesis:** An error introduced in `52cdb66` or later completely shattered the execution loop. `ReferenceError: id is not defined` in `SpatialCanvas.tsx` at line 162 broke the `TopTabBar` event loop previously. If clicking "Spawn Tab Here" from the canvas context menu does nothing, then `setOmnibarOpen(true, contextMenuPos)` is either failing to render the `<Omnibar />` component, or the state update is being swallowed.
**Actionable Fix:** 
1. Verify `<Omnibar />` is correctly mounted inside the React tree (likely inside `SpatialCanvas.tsx` or `App.tsx`) and not conditionally removed by the `isTopTabBarVisible` or `isFullScreen` patches.
2. Check `SpatialCanvas.tsx`'s `onPaneContextMenu` and ensure the coordinates (`contextMenuPos`) are successfully passing into `setOmnibarOpen`.
3. Check `Omnibar.tsx` to ensure `handleSubmit` correctly fires `addWidget` with the passed coordinates.
