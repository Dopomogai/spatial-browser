# Everything Broken 
**Issue:** The user stated "tabs are not spawning, shift is not working, the drop is not working properly on drag, I can no longer add it - basically nothing happens on clicking anything from the canvases right click menu."
**Hypothesis:** An error introduced in `52cdb66` or later completely shattered the execution loop. `ReferenceError: id is not defined` in `SpatialCanvas.tsx` at line 162 explicitly breaks the `TopTabBar` event loop. This implies `handleSpawnSettings` was fundamentally shattered in patching. The right-click menu actions *also* invoke Zustand `getState().addTextNode` or `setOmnibarOpen(true, pos)`. If `App.tsx` or `SpatialCanvas.tsx` is completely halting during React reconciliation, nothing works.
**Resolution:** Fix `SpatialCanvas.tsx` line 162. Re-examine the missing `id` definitions (`const id = \`settings_widget_${Date.now()}\``). Re-examine the missing `const id = \`canvases_widget_${Date.now()}\`` if it also was removed during viewport patch attempts.
