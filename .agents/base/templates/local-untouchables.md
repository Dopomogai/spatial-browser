# Untouchables

Things in this codebase that look weird but must NOT be changed without explicit human approval.

Each entry should explain what the thing is, why it's there, and where to look for context. Agents read this before making non-trivial changes.

<!-- TEMPLATE: add entries as you discover them. Repos typically have
     3-10 untouchables. If you have 50+, you're probably over-flagging. -->

## Example: <name of thing>

<!-- TODO: replace with real entries. -->

What it is and where: <description and file:line>

Why it's there: <reason — link to ADR, bug, or original incident>

What you should NOT do: <specific things that look reasonable but aren't>

What you should do instead: <if there's a correct path>

---

<!-- DELETE-WHEN-FILLED: this template was generated by `bin/librarian sync`
     during onboarding (Phase A4 of docs/21-onboarding.md).

     Add entries as you discover them. Common categories:
     - Workarounds for specific bugs (link the bug)
     - Non-obvious performance optimizations
     - Platform-specific quirk handlers
     - Security-sensitive boundaries
     - Code that looks like duplication but isn't
     - Magic constants that need to stay constant

     If a category has many entries, group with H2 sections. -->
